<%= link_to(
  '&larr; Intents'.html_safe,
  edit_skill_intent_path(@skill, @intent['name']),
  class: 'btn md grey pull-left'
) %>
<br><br>

<%= react_component(
  'Container',
  {
    intent_id: @intent.name,
    skill_name: @skill.name,
    web_hook: @skill.web_hook,
    field_path: fields_page_path(@skill, @intent['name']),
    fields: @fields
  },
  class: 'dialogs-container'
) %>
