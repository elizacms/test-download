<%= render partial: 'lock' %>

<%= react_component(
  'Container',
  {
    intent_id: @intent.id.to_s,
    intent_name: @intent.name,
    skill_name: @skill.name,
    web_hook: @skill.web_hook,
    field_path: fields_page_path(@skill, @intent),
    fields: @fields,
    file_lock: @intent.locked_for?(@current_user),
    edit_skill_intent_path: edit_skill_intent_path(@skill, @intent)
  },
  class: 'dialogs-container'
) %>
