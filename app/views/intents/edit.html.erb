<h1>Edit <%= @intent.name %> Details</h1>

<%= render partial: "form" %>

<% if flash.present? %>
  <div class="notice"><%= flash[:notice] %></div>
  <div class="alert"><%= flash[:alert] %></div>
<% end %>

<%= link_to "Delete this intent", skill_intent_path( @skill, @intent ), method: :delete, data: { confirm: "Are you sure you want to delete this intent?" } %>
