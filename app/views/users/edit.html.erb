<%= link_to(
  "<span class='icon-left-dir'></span> All Users".html_safe,
  users_path
) %>
<%= link_to(
  "Delete this user",
  user_path(@user),
  method: :delete,
  data: {
    confirm: "Are you sure you want to delete this user?"
  },
  class: "btn sm red pull-right"
) %>

<h1>Edit <%= @user.email %></h1>

<%= render partial: "form" %>

