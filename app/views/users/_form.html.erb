<div class="row">
  <div class="offset-sm-2 col-sm-8">
    <%= form_for @user do |f|%>
      <label>Email</label>
      <%= f.email_field :email %>
      <br>

      <%= f.submit 'Submit', class: "btn lg black pull-right" %>
    <% end %>
  </div>
</div>
